// TCP messaging server removed — all messaging now uses UDP unicast.
// This module is kept empty for backwards compatibility with mod declarations.
// TCP is only used for file data transfer (sender.rs / receiver.rs).
